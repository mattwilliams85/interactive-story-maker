import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  textArea: {
    height: 35,
    width: '100%',
    paddingLeft: 10,
    paddingTop: 10,
    paddingBottom: 10,
    paddingRight: 10,
    backgroundColor: '#fff',
    borderWidth: 1,
    borderColor: '#ccc',
  },
})
