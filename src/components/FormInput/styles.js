import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    input: {
        height: 37,
        width: '100%',
        paddingLeft: 10,
        backgroundColor: '#fff',
        borderWidth: 1,
        borderColor: '#ccc',
    },
})
