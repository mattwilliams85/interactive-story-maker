import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    paddingTop: 30,
    alignItems: 'center',
  },
  button: {
    textAlign: 'center',
    padding: 5,
    width: 300,
    marginBottom: 30,
    borderRadius: 4,
    borderWidth: 3,
  }
})
